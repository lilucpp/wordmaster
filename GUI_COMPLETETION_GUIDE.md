# GUI 完成指南

## 已创建的文件

### 主窗口
- `src/presentation/mainwindow.h`
- `src/presentation/mainwindow.cpp`

### 子组件
- `src/presentation/widgets/book_list_widget.h/cpp`
- `src/presentation/widgets/study_widget.h/cpp`
- `src/presentation/widgets/review_widget.h/cpp`
- `src/presentation/widgets/statistics_widget.h/cpp`

---

## 需要添加的 Repository 实现

以下仓储已在之前步骤中实现，需要确保包含正确：

```cpp
#include "infrastructure/repositories/book_repository.h"
#include "infrastructure/repositories/word_repository.h"
#include "infrastructure/repositories/study_record_repository.h"
#include "infrastructure/repositories/review_schedule_repository.h"
```

---

## 编译步骤

### 1. 更新 CMakeLists.txt

在根 `CMakeLists.txt` 中，确保包含所有 UI 文件：

```cmake
# 收集源文件
file(GLOB_RECURSE PRESENTATION_SOURCES 
    "${SRC_DIR}/presentation/*.cpp"
)

# 在 add_executable 中添加
add_executable(${PROJECT_NAME}
    ${SRC_DIR}/main.cpp
    ${DOMAIN_SOURCES}
    ${INFRASTRUCTURE_SOURCES}
    ${APPLICATION_SOURCES}
    ${PRESENTATION_SOURCES}  # 添加这行
)

# 如果使用 Qt Widgets 需要添加
target_link_libraries(${PROJECT_NAME}
    Qt5::Core
    Qt5::Widgets
    Qt5::Sql
)
```

### 2. 创建目录结构

```bash
mkdir -p src/presentation/widgets
```

### 3. 将 SQL 文件添加到资源

创建 `resources/resources.qrc`:

```xml
<!DOCTYPE RCC>
<RCC version="1.0">
    <qresource>
        <file>database/001_initial_schema.sql</file>
    </qresource>
</RCC>
```

在 CMakeLists.txt 中添加：

```cmake
set(CMAKE_AUTORCC ON)

qt5_add_resources(RESOURCES resources/resources.qrc)

add_executable(${PROJECT_NAME}
    ...
    ${RESOURCES}
)
```

---

## 编译命令

```bash
# 清理
make clean

# 重新构建
make build

# 运行
cd build
./WordMaster
```

---

## 功能说明

### 词库管理页面
- 显示所有已导入的词库
- 显示词库统计（总数、已学、掌握、进度）
- 点击"导入词库"按钮导入新词库
- 点击词库卡片激活该词库
- 点击"开始学习"进入学习页面

### 学习页面
- 显示单词和音标
- 点击"显示释义"查看含义和例句
- 点击"认识"或"不认识"标记并进入下一个
- 显示学习进度
- 完成后显示总结

### 复习页面
- 与学习页面类似
- 额外提供4个质量按钮
  - 🔴 遗忘：完全不记得
  - 🟡 困难：有点难
  - 🟢 良好：正常
  - 🔵 简单：很容易
- 根据质量调整复习间隔

### 统计页面
- 今日学习统计
  - 新学单词数
  - 复习单词数
  - 学习时长
- 所有词库进度
  - 进度条显示
  - 掌握情况

---

## 快速测试流程

### 1. 导入词库

```bash
# 启动应用
./build/WordMaster

# 点击"导入词库"按钮
# 选择：1764429285470_recommend_word.json
# 等待导入完成
```

### 2. 学习单词

```bash
# 在词库列表中选择 CET-4
# 点击"开始学习"按钮
# 或点击左侧导航"📖 学习"
# 学习20个单词
```

### 3. 复习单词

```bash
# 第二天打开应用
# 点击左侧导航"🔄 复习"
# 复习昨天学习的单词
```

### 4. 查看统计

```bash
# 点击左侧导航"📊 统计"
# 查看今日学习统计
# 查看词库进度
```

---

## 可能的编译问题

### 问题1: 找不到头文件

**症状：**
```
fatal error: presentation/mainwindow.h: No such file or directory
```

**解决：**
确保目录结构正确：
```
src/
├── presentation/
│   ├── mainwindow.h
│   ├── mainwindow.cpp
│   └── widgets/
│       ├── book_list_widget.h
│       ├── book_list_widget.cpp
│       ├── study_widget.h
│       ├── study_widget.cpp
│       ├── review_widget.h
│       ├── review_widget.cpp
│       ├── statistics_widget.h
│       └── statistics_widget.cpp
```

### 问题2: 链接错误

**症状：**
```
undefined reference to `WordMaster::Presentation::MainWindow::MainWindow()'
```

**解决：**
确保 CMakeLists.txt 正确收集了所有 .cpp 文件：
```cmake
file(GLOB_RECURSE PRESENTATION_SOURCES 
    "${SRC_DIR}/presentation/*.cpp"
)
```

### 问题3: Qt MOC 错误

**症状：**
```
error: undefined reference to `vtable for WordMaster::Presentation::MainWindow'
```

**解决：**
确保设置了：
```cmake
set(CMAKE_AUTOMOC ON)
```

并重新构建：
```bash
rm -rf build
make build
```

---

## 后续优化

### P1 功能（可选）

1. **标签管理**
   - 生词本
   - 错误本
   - 收藏本

2. **学习计划**
   - 设置每日目标
   - 进度追踪

3. **更多统计**
   - 学习日历热力图
   - 详细图表

4. **设置页面**
   - 每日新词数
   - 每日复习数
   - 主题切换

### UI 美化

1. **更好的样式**
   - 使用 QSS 统一样式
   - 添加图标
   - 优化布局

2. **动画效果**
   - 页面切换动画
   - 进度条动画

3. **快捷键**
   - Space: 显示释义
   - 1/2: 不认识/认识
   - ←/→: 上一个/下一个

---

## 测试检查清单

- [ ] 应用可以正常启动
- [ ] 可以导入词库
- [ ] 词库列表显示正确
- [ ] 可以激活词库
- [ ] 可以开始学习
- [ ] 单词卡片显示正确
- [ ] 可以标记认识/不认识
- [ ] 学习进度正确
- [ ] 可以开始复习
- [ ] 复习质量按钮工作
- [ ] 统计数据正确显示
- [ ] 数据持久化正常

---

## 截图（预期效果）

### 词库管理
```
┌────────────────────────────────────────────┐
│ 词库管理              [刷新] [导入词库]    │
├────────────────────────────────────────────┤
│ ┌────────────────────────────────────────┐ │
│ │ CET-4              ✓ 当前词库 [开始学习]│ │
│ │ 📂 中国考试  🏷️ 大学英语               │ │
│ │ 📚 总数: 2607  ✅ 已学: 120  ⭐ 掌握: 50│ │
│ │ ████████░░ 46%                         │ │
│ └────────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

### 学习界面
```
┌────────────────────────────────────────────┐
│ 学习新单词 (5/20)                          │
│ ████████████████░░░░  5 / 20               │
├────────────────────────────────────────────┤
│                                            │
│              explosive                     │
│        /ɪkˈspləʊsɪv/ / /ɪkˈsploʊsɪv/     │
│                                            │
│           [显示释义 ↓]                     │
│                                            │
│         [释义内容]                         │
│                                            │
│         [❌ 不认识]  [✅ 认识]             │
└────────────────────────────────────────────┘
```

---

## 下一步

构建并运行应用：

```bash
make build
cd build
./WordMaster
```

如遇到问题，查看编译日志，根据错误提示修复。

祝开发顺利！🚀